# bootstrap.yml可以理解成系统级别的一些参数配置，这些参数一般是不会变动的
spring:
  application:
    name: admin

management:
  security:
    enabled: false
endpoints:
  hypermedia:
    enabled: true
  restart:
    enabled: true
  refresh:
    enabled: true
  health:
    sensitive: false
#  docs:
#    enabled: true


